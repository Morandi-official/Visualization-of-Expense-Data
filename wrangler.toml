name = "visualization-of-expense-data"
compatibility_date = "2026-06-06"
pages_build_output_dir = "."

# D1 数据库绑定请在 Cloudflare Pages 控制台中配置：
# Pages 项目 -> Settings -> Bindings -> Add -> D1 database bindings
# 变量名必须填写 DB，并选择你的 expense-data-db 数据库。
# 不在这里放占位 database_id，避免部署时报 Invalid database UUID。
